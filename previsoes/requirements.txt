anthropic>=1.0
pydantic>=2.0
requests>=2.31
# yt-dlp é chamado por subprocess — instale à parte:
#   pipx install yt-dlp    (ou: pip install yt-dlp)
# Testes: pip install pytest  (os testes usam só fixtures offline)
